<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="be.helha.degreve">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activities.GetLivresActivity" />
        <activity android:name=".Activities.GetLivreActivity" />
        <activity android:name=".Activities.GetMagazinesActivity" />
        <activity android:name=".Activities.GetPublicationsActivity" />
        <activity android:name=".Activities.GetMagazineActivity" />
        <activity android:name=".Activities.GetAdaptationsActivity" />
        <activity android:name=".Activities.GetAuteursActivity" />
        <activity android:name=".Activities.GetEditeursActivity" />
        <activity android:name=".Activities.GetEditeurActivity" />
        <activity android:name=".Activities.GetAuteurActivity"></activity>
    </application>

</manifest>